export type loginForm = {
    username : string;
    password: string;
} 

export type signUpForm = {
    username: string;
    email: string;
    confirmEmail: string;
    password: string;
}